export type ConfirmDialogOptions = {
  title: string;
  message?: string;
  confirmLabel?: string;
  cancelLabel?: string;
  destructive?: boolean;
};

export type AlertDialogOptions = {
  title: string;
  message?: string;
  buttonLabel?: string;
};
